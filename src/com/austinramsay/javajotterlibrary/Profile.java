package com.austinramsay.javajotterlibrary;

import java.util.HashMap;

/**
 *
 * @author aramsay
 * A profile needs to contain a list of notebooks associated to the user, and
 * it needs to contain what notes are associated with the notebooks.
 */
public class Profile {
		// im not sure this is necessary since server database holds this info
}
